/** Shared beige background for student booking flows (individual / joint availability). */
export default function AvailabilityLayout({ children }) {
  return <div className="student-app-root min-h-screen">{children}</div>;
}
